# Economics and Waste

This area contains measured cost, yield, cost-per-serve, waste, utilisation, and menu-application models. Retain price dates, package sizes, actual quantities used, measured yields, and calculation logic.

Do not enter estimates as measurements. Mark scenario assumptions explicitly and keep them separable from observed values.

