{
  "name": "cordials-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Local-first dashboard for the Cordials repository",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
